import { ModerationQueue } from "@/components/features/admin/moderation-queue";

export const metadata = {
  title: "Moderation",
};

export default function AdminModerationPage() {
  return <ModerationQueue />;
}
