/** Toggle discovery search bar (not part of MVP discovery experience). */
export const DISCOVERY_SEARCH_ENABLED = false;

/**
 * Portrait aspect for discovery card photos — ~30% shorter than the default 3/4
 * portrait used elsewhere, so the card fits a swipe-first mobile viewport.
 */
export const DISCOVERY_CARD_PHOTO_ASPECT = "aspect-[15/14]" as const;
